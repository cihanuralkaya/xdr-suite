name: CI

on:
  push:
    branches: [main, master]
  pull_request:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  test:
    name: Vet + Test + Smoke
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-go@v5
        with:
          go-version: "1.23"
          cache: true

      # Proto kodunu üret (gen/ commit edilmez; buf ile üretilir).
      - uses: bufbuild/buf-setup-action@v1
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
      - name: protoc-gen eklentileri
        run: |
          go install google.golang.org/protobuf/cmd/protoc-gen-go@latest
          go install google.golang.org/grpc/cmd/protoc-gen-go-grpc@latest
      - name: Proto üret
        run: make proto

      - name: gofmt kontrolü
        run: |
          unformatted="$(gofmt -l .)"
          if [ -n "$unformatted" ]; then
            echo "Biçimsiz dosyalar (gofmt -w ile düzeltin):"; echo "$unformatted"; exit 1
          fi

      - name: go vet
        run: go vet ./...

      - name: go test
        run: go test ./...

      - name: Uçtan uca smoke test
        run: bash scripts/smoke-test.sh

  db-test:
    name: DB yolu (PostgreSQL)
    runs-on: ubuntu-latest
    needs: test
    services:
      postgres:
        image: postgres:16
        env:
          POSTGRES_USER: xdr
          POSTGRES_PASSWORD: xdr
          POSTGRES_DB: xdr
        ports:
          - 5432:5432
        options: >-
          --health-cmd "pg_isready -U xdr"
          --health-interval 5s
          --health-timeout 5s
          --health-retries 10
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-go@v5
        with:
          go-version: "1.23"
          cache: true
      - uses: bufbuild/buf-setup-action@v1
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
      - name: protoc-gen eklentileri
        run: |
          go install google.golang.org/protobuf/cmd/protoc-gen-go@latest
          go install google.golang.org/grpc/cmd/protoc-gen-go-grpc@latest
      - name: Proto üret
        run: make proto
      - name: psql istemcisi
        run: sudo apt-get update && sudo apt-get install -y postgresql-client
      # smoke-test.sh, XDR_DATABASE_URL görünce şemayı yükler, admin tohumlar ve
      # tüm zinciri GERÇEK PostgreSQL'e karşı çalıştırır (pgx yolu).
      - name: DB uçtan uca smoke test
        env:
          XDR_DATABASE_URL: postgres://xdr:xdr@localhost:5432/xdr?sslmode=disable
        run: bash scripts/smoke-test.sh

  release:
    name: Çapraz derleme (release)
    runs-on: ubuntu-latest
    needs: test
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-go@v5
        with:
          go-version: "1.23"
          cache: true
      - uses: bufbuild/buf-setup-action@v1
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
      - name: protoc-gen eklentileri
        run: |
          go install google.golang.org/protobuf/cmd/protoc-gen-go@latest
          go install google.golang.org/grpc/cmd/protoc-gen-go-grpc@latest
      - name: Proto üret
        run: make proto
      - name: Release ikilileri
        run: bash scripts/build-release.sh "${GITHUB_REF_NAME}"
      - uses: actions/upload-artifact@v4
        with:
          name: xdr-binaries
          path: dist/
          if-no-files-found: error
